#include<iostream>

int main()
{
    int * p = nullptr;
    //  "int * p = nullptr" is equal to "int * p = 0" that mean " p points to nothing".  
    std::cout << p << std::endl;
    
    return 0;
}
